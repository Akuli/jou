on:
  push:
    branches:
      - main
  pull_request:

jobs:
  doctest:
    runs-on: macos-latest
    timeout-minutes: 5
    steps:
      - run: |
          env | grep ARCH
          env | grep 64
          uname -m
          uname -a
