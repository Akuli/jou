import "stdlib/io.jou"

def main() -> int:
    point1 = [1, 2]
    point2 = [3, 4]
    added = point1 + point2

    # Output: 4
    # Output: 6
    for i = 0; i < array_count(added); i++:
        printf("%d\n", added[i])

    return 0
