# This hello world program will work even if there is a problem with importing
# stdlib/io.jou. It is sometimes useful when debugging the Jou compiler. It
# might also help someone understand how `declare` works.

declare puts(s: byte*) -> int

def main() -> int:
    puts("Hello World")
    return 0
