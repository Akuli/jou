# Make sure that your editor doesn't "clean up" whitespaces in this file :)
# It must end with 3 spaces and no newline character.
# The purpose is to make sure the compiler doesn't complain.

import "stdlib/io.jou"

def main() -> int:
    return 0
   