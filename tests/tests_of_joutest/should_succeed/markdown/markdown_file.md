# Test Document

This is a test.

```jou
import "stdlib/io.jou"
def main() -> int:
    puts("Hello from markdown")  # Output: Hello from markdown
    return 0
```

Bla bla bla. This is completely ignored.

The following codeblock is ignored, because the specified language is wrong:

```js
console.log("this does not run");  # Output: whatever really
```
