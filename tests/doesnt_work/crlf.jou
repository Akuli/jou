# This file has CRLF line endings. Make sure your editor saves it as such.
# It also contains this comment, and so it cannot be ran currently due to a bug...
cdecl putchar(ch: int) -> int

def main() -> int:
    # Output: hi
    putchar('h')
    putchar('i')
    putchar('\n')
    return '\0'
