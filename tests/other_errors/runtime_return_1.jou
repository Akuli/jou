import "stdlib/io.jou"

def main() -> int:
    # Make sure this code actually runs, so it isn't a compile error
    putchar('x')  # Output: x
    putchar('\n')

    # Returning 1 from main makes the process exit with code 1, so it counts as a failure.
    return 1
