# TODO: The error message is misleading.
# Before introducing generics, it used to be: cannot evaluate array length at compile time

def foo(a: int, b: int[a]) -> int:  # Error: there is no generic class named 'int'
    return 0
