class Foo:
    def bar(self) -> None:
        pass


def foo() -> None:
    f = Foo{}
    f_ptr = &f
    thing = f_ptr.bar  # Error: Foo.bar() is a method, it can be called but it cannot be used in any other way
