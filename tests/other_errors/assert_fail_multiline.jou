def main() -> int:
    lol = True
    wat = True
    assert (  # Output: Assertion '(lol and not wat)' failed in file "tests/other_errors/assert_fail_multiline.jou", line 4.
        lol
        and not wat
    )

    # No need for a "return 0" because compiler knows we will never get here.
