import "./imported/privates.jou"

def main() -> int:
    # TODO: error message could be better, should say that it exists but is not @public
    x = PRIVATE_CONSTANT  # Error: no variable named 'PRIVATE_CONSTANT'
    return 0
