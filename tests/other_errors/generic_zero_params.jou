class Foo[T]:
    pass

def foo() -> None:
    # Error message talks about arrays rather than generics. IMO that's fine
    # because the same syntax is much more commonly used for arrays.
    x: Foo[]  # Error: array length must be specified, e.g. int[10] instead of int
