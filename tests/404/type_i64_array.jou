const SIZE: int = 10

# This is in a separate test because the error comes from a different place in the compiler.
def foo(x: i64[SIZE]) -> None:  # Error: there is no type named 'i64', use 'int64' instead
    pass
