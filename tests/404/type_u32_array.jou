const SIZE: int = 10

# This is in a separate test because the error comes from a different place in the compiler.
def foo(x: u32[SIZE]) -> None:  # Error: there is no type named 'u32', use 'uint32' instead
    pass
