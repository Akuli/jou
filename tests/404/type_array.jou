# This is tested separately from other type not found errors, because the
# compiler creates this error in a different place than other type not
# found errors.
def foo() -> asd[10]:  # Error: there is no type named 'asd'
    return 1
