const SIZE: int = 10

# This is in a separate test because the error comes from a different place in the compiler.
def foo(x: uint[SIZE]) -> None:  # Error: there is no type named 'uint', use 'uint32' instead
    pass
