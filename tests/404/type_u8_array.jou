const SIZE: int = 10

# This is in a separate test because the error comes from a different place in the compiler.
def foo(x: u8[SIZE]) -> None:  # Error: there is no type named 'u8', use 'byte' instead
    pass
