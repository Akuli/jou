struct Foo:
    x: int

    def bar(self: Foo) -> void:
        return

def asdf() -> void:
    f = Foo{x=1}
    f.bar()
    (&f)->bar()
    (&f).bar()  # Error: the method 'bar' is defined for type Foo, not for type Foo*, so you need to dereference the pointer first (e.g. by using '->' instead of '.')
