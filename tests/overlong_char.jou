def main() -> int:
    # TODO: The error message below mentions "double-quoted" strings
    #       even though they don't exist yet.
    return 'hi'  # Error: single quotes are for a single character, maybe use double quotes to instead make a string?
