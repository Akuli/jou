# Only structs can have methods in Jou.

def foo() -> void:
    (1 + 2).asdf()  # Error: type int does not have a method named 'asdf', and in fact, it doesn't have any methods because it is not a struct
