# TODO: Allow evaluating 'as' at compile time. This should cause no errors.
#const GOOD_INF: float = (1.0 as float) / (0.0 as float)

# This must not convert the ints to float, because that's not what would
# happen if this code without the "const" was inside a function
const BAD_INF: float = 1 / 0  # Error: cannot evaluate value of constant at compile time
