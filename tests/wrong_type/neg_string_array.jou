def bruh() -> None:
    thing: byte[50] = "Hello"

    # You can do e.g. -[1, 2] to negate each element, but this would be too
    # surprising for Jou.
    thing2 = -thing  # Error: value after '-' must be a signed number or an array of signed numbers, not byte[50]
