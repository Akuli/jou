# Only BSD make uses this file! If you are wondering what "make" does on
# your system, you probably want to look in GNUmakefile instead (unless
# you're on NetBSD).
#
# This file tells BSD make to use the correct makefile. We use BSD make
# on NetBSD, because NetBSD comes with it.

.include "Makefile.posix"
